# Commit Plan — Grow My Plant

## Repo state
- Branch: `master`
- Local/remote: in sync at `cc75c0b`
- Uncommitted changes: 14 modified, 12 untracked

## Proposed commits

### 1. feat: harden Instagram webhook ingestion and event logging
Files:
- `app/api/instagram/webhook/route.ts`
- `lib/instagram/verifyWebhook.ts`
- `lib/instagram/parseCommentEvent.ts`
- `lib/leaves/processComment.ts`
- `scripts/verify-instagram-webhook.ts`
- `scripts/verify-sig-prod.ts`
- `scripts/test-webhook.ts`

Why:
- Add HMAC signature verification (`X-Hub-Signature-256`)
- Parse verified webhook payloads into structured comment events
- Return explicit webhook status/result instead of generic `ok`
- Improve event/failure observability for production debugging

### 2. feat: refine leaf placement algorithm
Files:
- `lib/plant/leafPlacement.ts`

Why:
- Refine deterministic leaf allocation onto branches
- Improve canopy distribution without changing rendering

### 3. feat: update growth stage definitions
Files:
- `lib/plant/growthStages.ts`

Why:
- Adjust stage thresholds or naming independently from placement math

### 4. feat: stabilize realtime leaf stream updates
Files:
- `lib/realtime/subscribe.ts`

Why:
- Keep live broadcast/client subscription aligned with repo changes

### 5. feat: refresh garden rendering and interactions
Files:
- `components/CommentGarden.tsx`
- `components/PlantCanvas.tsx`
- `components/PlantClient.tsx`

Why:
- Tie new slot geometry and stage transitions into the UI
- Include interaction/search/zoom updates in one focused render commit

### 6. feat: add pixel bunny visual asset
Files:
- `components/PixelBunny.tsx`
- `lib/plant/pixelBunnyFrames.ts`

Why:
- Isolate aesthetic/asset addition from logic changes

### 7. feat: polish garden UI and campaign surfaces
Files:
- `app/globals.css`
- `app/admin/page.tsx`

Why:
- Add the call-to-action box for live growth
- Improve admin dashboard styling while keeping layout behavior intact

### 8. chore: config, docs, and cleanup
Files:
- `lib/config.ts`
- `.env.example`
- `README.md`
- `scripts/debug-db.ts`
- `scripts/fix-campaign-media.ts`
- `scripts/hello-print.ts`
- `scratch_capacity.cjs`
- `test-regex.js`

Why:
- Document/config values that existing admin flows rely on
- Capture utility scripts and scratch work without mixing them into runtime commits

## Suggested commit messages
1. `feat: harden Instagram webhook ingestion and event logging`
2. `feat: refine leaf placement algorithm`
3. `feat: update growth stage definitions`
4. `feat: stabilize realtime leaf stream updates`
5. `feat: refresh garden rendering and interactions`
6. `feat: add pixel bunny visual asset`
7. `feat: polish garden UI and campaign surfaces`
8. `chore: config, docs, and cleanup`

## Advice
- Consider `.agent/` and `.agents/` for `.gitignore` or removal if they are not intended for commits.
- After staging, run: `npm run lint` or `npm run build` before final review.
